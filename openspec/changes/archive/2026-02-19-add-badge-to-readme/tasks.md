## 1. CI Workflow

- [x] 1.1 Create `.github/workflows/openspec-badge.yml` that runs the action on push to main using `./` with `contents: write` permissions

## 2. README updates

- [x] 2.1 Add all four badge images side by side below the title (number_of_specs, number_of_requirements, tasks_status, open_changes) using raw.githubusercontent.com URLs
- [x] 2.2 Add `badge_style` row to the configuration table
- [x] 2.3 Add `permissions: contents: write` to the usage workflow YAML example
- [x] 2.4 Add "Displaying badges" section explaining the URL pattern and markdown syntax for embedding

## 3. Verify

- [x] 3.1 Review README and workflow against all specs — all requirements covered
